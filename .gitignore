# Secrets
.env

# Runtime state and logs — เขียนลง data/ ข้างๆ CSV ที่ commit ไว้
data/bot_state.json
data/bot_state.json.tmp
data/bot.log
data/bot.log.*

# ปฏิทินข่าวที่โหลดมาแคชไว้ — โหลดใหม่ได้เสมอ ไม่ใช่ข้อมูลที่บอทสร้าง
data/news_calendar.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# CSV ที่เขียนค้างตอนจัดคอลัมน์ใหม่
*.csv.tmp
